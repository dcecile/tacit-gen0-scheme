% Euro-Latin characters
\usepackage{fontspec}

% Multiline comments
\usepackage{comment}

% PDF hyperlinks
\usepackage{hyperref} 
\hypersetup{
  final,
  colorlinks,
  plainpages=false,
  bookmarksnumbered}

% Show index entries in margin
\ifdraftdoc\showindexmarks\fi

% Generate an index
\makeindex

% Special code-verbatim environment
\newenvironment{ccode}{
  \setlength{\topsep}{0pt}
  \setlength{\partopsep}{0pt}
  \setlength{\parskip}{0pt}
  \verbatim}{
  \endverbatim}

% Sidebar personal note
\setlength{\sidebartopsep}{11\onelineskip}
\setlength{\sidebarhsep}{20pt}
\newcommand*{\pnote}[1]{\sidebar{\textit{Personal note:\/} #1}}

% For advanced chapter styles
\usepackage{graphicx}

% Chapter styling
\chapterstyle{hangnum}
\setlength{\afterchapskip}{1.5\onelineskip}
\hangsecnum
