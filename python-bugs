- lexically scoped lambda:
  y = []
  for x in range(5):
    y.append(lambda z: z + x)
  print y[0](1)
  x = 10
  print y[0](1)
- leaky for loops
- static objects, they're just for indexing pre-bound methods
- syntactically stunted lambdas
- anti-interactive (module references, classes, REPL pickling)
- return keyword
- print keyword
- unhackable (Designed) base types
- impure generators:
  from itertools import repeat
  x = repeat('a', 4)
  y = list(x)
  z = list(x)
  assert(y == z)
